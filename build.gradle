apply plugin: 'maven'

group = 'com.github.giovannibaratta'

subprojects {
    dependencies{

    }

    repositories{
        mavenCentral()
        google()
    }

    apply plugin : 'idea'
    apply plugin : 'maven'

    group 'it.baratta.giovanni.habitat'
    version '0.2-SNAPSHOT'
}